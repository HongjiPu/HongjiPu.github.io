# 新闻配图 / News images

把 `_data/news.yml` 里 `image:` 用到的图片放在这个目录，然后这样引用：

```yaml
image: /images/news/你的文件名.jpg
```

**建议**：横图，短边不小于 400px。页面会按 4:3 裁切成缩略图。

`sample-paper.jpg` 和 `sample-code.jpg` 是占位图，换成你自己的图片后可以删掉。
